const NoItemsFound = {
  extend: 'Flex',
  text: 'No Items found',
  props: (el, s) => ({
    justifyContent: 'center',
    align: 'center',
    width: '100%',
    height: '200px',
    fontSize: '24px',
  }),
};
